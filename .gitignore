# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.local

# TS build info
*.tsbuildinfo

# Local-only env overrides (the committed .env carries the PUBLIC Supabase URL +
# anon key, which the build needs; secrets never live in this repo).
.env.local
